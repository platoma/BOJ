#include <stdio.h>
#include <math.h>
int main()
{
	int arr[5];
	int verification = 0;
	
	for (int i = 0; i < 5; i++)
	{
		scanf("%d", &arr[i]);
	}
	
	for (int i = 0; i < 5; i++)
	{
		verification += pow(arr[i], 2);
	}
	verification %= 10;
	
	printf("%d", verification);
}
